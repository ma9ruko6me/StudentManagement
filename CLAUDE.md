# 開発ルール

このプロジェクトは就職活動用ポートフォリオであり、GitHub上でIssue駆動・PRベースの開発を行う。Claude Codeは以下のルールを守ること。

## 1. Issue駆動

- 実装作業(機能追加・修正・設定変更など)を始める前に、必ず対応するGitHub Issueを作成する(`gh issue create`)。
- Issue本文には「何を」「なぜ」行うのかを簡潔に記載する。作成前に本文をユーザーに提示し、確認を得てから作成する(グローバル設定に準拠)。

## 2. ブランチ命名規則

`種別/issue番号-概要` の形式(kebab-case、英語)で作成する。

- `feature/12-add-course-search` — 新機能
- `fix/15-status-transition-bug` — バグ修正
- `chore/20-update-deps` — 雑務・設定変更
- `docs/22-split-readme` — ドキュメント更新

## 3. mainへの直接push禁止

常にIssue用のブランチ上で作業する。`main`ブランチへの直接コミット・直接pushは行わない。

## 4. PRフロー

1. Issue用ブランチを作成
2. 実装
3. `gh pr create`でPRを作成し、本文にIssueをリンクする語句(例: `Closes #12`)を含める
4. ユーザーの確認を得てPRをマージ

単独開発のためレビュー承認フローは形式的なものだが、Issue→ブランチ→PRのプロセス自体は省略しない。

## 5. バックエンド/フロントエンドの役割分担

- **バックエンド**: ユーザー自身が実装する。Claude Codeは実装を代筆せず、レビュー役(設計の妥当性・命名・テストの過不足・セキュリティ観点の指摘)に徹する。概念説明のための短いコード例の提示は可。レビューは基本1機能・PR単位で行うが、実装中に分からない箇所があれば都度質問に応じる。
- **フロントエンド**: ユーザーは未経験のため、Claude Codeが実装を担当する。書いたコードの意図・仕組みを都度説明し、基礎理解につなげる。

## 6. ローカルサーバー起動時のポート固定

フロントエンド追加後、バックエンド・フロントエンドのローカル開発サーバーを同時に動かす際は、既定ポートで起動する。ポート競合時は占有プロセスを停止してから既定ポートで起動し、別ポートへのフォールバックは行わない。(フロントエンド着手時に具体的なポート番号と起動手順をここに追記する)

# AIとの協働プロセス

就職活動用ポートフォリオとして、「AIに丸投げしない」進め方を明示的なルールとする。面接で「なぜこう作ったか」を自分の言葉で説明できる状態を保つことを最優先の目的とする。

## 1. 要件は先に言語化する(実装より先に質問させる)

新しい機能や変更に着手する前、Claude Codeはいきなり実装や具体的なコード案を出さない。まず要件の曖昧な点・目的・制約・仕様を質問し、ユーザーとの対話で要件を固めてから着手する。固まった要件は、可能であれば対応するIssue本文やPR本文に残す(「なぜこの仕様にしたか」を後から追える状態にする)。

## 2. 実装前に計画書(設計メモ)を作る

規模のある変更(新規機能・設計変更・インフラ変更など)では、いきなり実装に入らず、以下を先にマークダウンで整理してユーザーの確認を得る。

- どのファイルを変更・追加するか
- テスト方針
- 完了条件

軽微な修正(typo、単純なバグ修正など)ではこの手順を省略してよい。

## 3. 自分の言葉で説明できる状態を保つ

Claude Codeが提示した設計判断について、ユーザーが「なぜそうしたか」「そうしなかったら何が起きるか」を自分の言葉で説明できることを基準にする。レビューやペアプロの中で、ユーザーが理解せずに実装を進めていると判断した場合は、先に進めず理由を確認する。

## 4. 設計判断の記録を残す

「なぜこのライブラリ/実装方針を選んだか」を、`docs/`配下のドキュメントまたはREADMEに一言でも残す。技術選定や設計変更を行う際、Claude Codeは記録を残すことを都度促す。

## 優先度を下げてよいもの

Cursor Cloud Agentsのような複数AIモデルによるレビューループや、CodeRabbit等の有料レビューツールは、就活段階では導入しない。ツールよりも上記のプロセス(要件の言語化・計画書・自分の言葉での説明・設計判断の記録)を優先する。

# 関連ドキュメント

- 要件定義: docs/requirements.md (作成予定)
- 基本設計: docs/basic-design.md (作成予定)
- インフラ構成: docs/infrastructure.md (作成予定)
